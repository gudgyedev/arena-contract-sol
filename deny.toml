[advisories]
ignore = [
    { id = "RUSTSEC-2025-0141", reason = "Accepted for this release candidate because bincode 1.3.3 is inherited through Solana/SPL normal dependencies, the advisory is unmaintained rather than a memory-safety CVE, and Solana 4.x still carries the same advisory in the on-chain dependency graph." },
    { id = "RUSTSEC-2025-0161", reason = "Accepted for this release candidate because libsecp256k1 0.6.0 is inherited through solana-program 2.x, the workspace does not call secp256k1 recovery directly, and the affected path is Solana ecosystem code rather than custom program logic." },
]
